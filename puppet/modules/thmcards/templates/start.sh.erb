#!/usr/bin/env bash

echo -e "Starting THMcards...\c"

OUTPUT_REDIRECT="2>/dev/null 1>&2"
VERBOSE=""
while getopts v opt
do
  case $opt in
    v)
      OUTPUT_REDIRECT=""
      VERBOSE="-v"
      ;;
  esac
done
shift $((OPTIND - 1))

eval "cd /vagrant"
eval "nodejs app.js"

